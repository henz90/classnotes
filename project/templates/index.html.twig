{% extends "master.html.twig" %}

{% block title %}Main Page{% endblock %}

{% block content %}
    {% for c in list %}
    <div id="class">
        <h1><a href="/class/{{c.classid}}">{{c.classname}}</a></h1>
        <p>Created by <a href="/class/{{u.userid}}">{{u.username}}</a></p>
        <p>{{c.body}}</p>
    </div>
    </br>
    {% endfor %}
{% endblock %}